# C1 — Forgotten web

<!-- This entry has to be yours: it is the one part of the submission that is
     an account of your own thinking, and the pod hears the first 30 seconds of
     it out loud. What follows is a prompt sheet, not a draft. Replace it.
     150–300 words is plenty. -->

## What was the breakthrough that moved the work forward?

<!-- Candidates from this week, if one of them is honestly the breakthrough.
     Pick ONE and write it properly; a list is not a reflection.

     - The phone "bug" that wasn't. The 390px screenshot showed the layout
       overflowing and the obvious move was to start patching CSS. Building a
       media-query probe page first proved the harness was lying — headless
       Chrome on macOS clamps the viewport to ~500px and crops the image — so
       the fix went into how the site gets measured (an iframe harness), not
       into the stylesheet. Ask yourself: what would the CSS look like now if
       I had trusted the screenshot?

     - Turning "no JavaScript" from a rule I was trying to remember into a test
       that fails the build. Which of those two actually holds a constraint?

     - Deciding the guestbook would have no form. The era-correct answer and
       the no-JS constraint pointed the same way, and the constraint made the
       content better rather than poorer. -->

## What did this work change about who I want to be as a software developer?

<!-- The question is about you, not the prototype. Some threads worth pulling:

     - You directed this build without writing the HTML yourself. What did you
       have to get good at instead, and is that a skill you want?
     - The corrections that stuck went into CLAUDE.md rather than into another
       prompt. What does it change about how you work if the durable artefact
       is the harness and not the code?
     - You were blocked on a repo you did not control and built anyway. -->
